# T27 书框架改写抽查报告

> 任务：核对 `cc-assistant/modules/capstone.md` 体系讲解无书原文整段照抄、无原章节结构/句式、归因标注（书框架复用边界判定标准：不整段照抄、不保留原章节结构/句式）。
> 对照源：`docs/harness-章节总结.md`（L8-17 四层架构表 + 触发口诀）、`docs/book-harness-summary.md`（L16-18 核心心智模型）。

## 1. 抽查范围与比对方法

- 提取 capstone.md「体系讲解（改写归因）」与「讲关注点分离」两处文本，与两本书素材逐句比对：
  - 四层架构：记忆层 / 扩展层 / 集成层 / 编程层
  - 触发口诀
  - 关注点分离
  - 选型决策树
- 检查三维度：整段照抄 / 章节结构复用 / 句式重复；以及归因标注是否存在。

## 2. 比对结果

### 四层架构

| 书素材 | capstone.md 交付物 | 判定 |
|---|---|---|
| 记忆层 CLAUDE.md 地基「让模型不失忆」 | 记忆层（CLAUDE.md 为模型提供项目级持久记忆） | 概念同、表述改 |
| 扩展层 Commands/Skills/SubAgents/Hooks 主体楼层 扩展能力 | 扩展层（Commands/Skills/子智能体/Hooks 扩展能力） | 概念同、通用词 |
| 集成层 Headless/MCP 水电管网 连接外部世界 | 集成层（MCP/Headless 连接外部） | 概念同、表述改 |
| 编程层 Agent SDK 顶楼建筑师工作室 | 编程层（Agent SDK 构建应用） | 概念同、删比喻、表述改 |

### 触发口诀

- 书：Commands「你叫它做」、Skills「它自己知道该做」、子智能体「它安排别人做」、Hooks「不管谁做，到这一步就检查」。
- capstone：命令靠人手动唤出、Skill 靠描述让 Claude 自动匹配、子智能体靠点名或委派、Hooks 靠事件到点自动拦。
- **判定**：同一概念（按触发者/触发方式分类），完全不同的口头表达。无句式重复。

### 关注点分离

- 书：CLAUDE.md 知道什么（Know-what）、Skills 怎么做（Know-how）、子智能体谁来做（Who-does-it）、Hooks 能否做（Guard-rails）。
- capstone：记忆与规则负责「项目知道什么」，Skill 承载「重复步骤怎么做」，子智能体处理「哪个子任务交给谁」，Hooks 把关「这一步能不能放行」。
- **判定**：同概念、自有表达，结构（并列排比）为教学通用形式，非书章节结构。

### 选型决策树

- 书素材仅提及「选型决策树」作为参考路线；capstone 表述「选型决策树（按场景选组件）」，为课程自有归纳。无照抄。

## 3. 发现与处置

- **整体判定：PASS**。无整段照抄、无原章节结构/句式复用；四层架构/触发口诀/关注点分离均为「同概念、自有表达」改写，并明确归因（capstone.md L15 内联 + L33 交叉引用「体系框架来源：黄佳《Claude Code 实战》…课程自有改写并归因，未内联书原文」）。
- **1 处 Minor（已修复）**：原「记忆层（CLAUDE.md 让模型不失忆）」中「让模型不失忆」与书章节总结 L12 逐字重复（书中带引号特色口号）。已改写为「CLAUDE.md 为模型提供项目级持久记忆」，消除逐字残留。其余「扩展能力」「连接外部」为通用词，不构成复用边界违规。
- 书章节结构（L8-15 表格 + 逐章总结）未被复制；capstone 以教学叙事组织，结构不同。

## 4. 结论

书框架复用边界判定标准满足：不整段照抄、不保留原章节结构/句式、归因标注齐全。逐字残留 1 处已收敛，遗留 0 项。
